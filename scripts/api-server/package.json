{
  "name": "minecraft-bot-api-server",
  "version": "2.0.0",
  "description": "HTTP API wrapper for minecraft-bot. Exposes local MC instance operations over HTTP.",
  "type": "commonjs",
  "main": "index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": ">=18"
  }
}
